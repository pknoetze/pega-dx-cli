// Reserved for per-endpoint response interfaces introduced in Phase 2b.3
// when the --interactive wizard reads the action view. Phase 2b.1 commands
// treat responses as `unknown` and emit pass-through.
export {};
